package com.atlasck.repository;

public interface CurrentVersion {

	/**
	 * Returns CurrentVersion by hardcoded id = 1
	 *
	 * @param id
	 * @return CurrentVersion
	 */
	CurrentVersion getCurrentVersion (Integer id);
}
